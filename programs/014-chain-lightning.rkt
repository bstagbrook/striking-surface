#lang strike

;;; ═══════════════════════════════════════════════════════════════
;;; CHAIN LIGHTNING — CONTINUOUS COLLAPSE CASCADE
;;;
;;; Every domain registered so far lives at a different depth
;;; in this single strike table. The Tesla fires at the core.
;;; Numbers resolve. Logic resolves using numbers. Alphabet
;;; resolves using logic. Each domain's residue IS the next
;;; domain's input.
;;;
;;; One strike. Every domain collapses. The cascade IS
;;; the demonstration of the language continuum.
;;;
;;; This is not a pipeline. There is no clock between stages.
;;; The nesting depth IS the causal structure. Inner collapses
;;; before outer because the shape REQUIRES it. Not because
;;; anything scheduled it.
;;; ═══════════════════════════════════════════════════════════════

(chain-lightning

  ;;; Source: the entire cascade, innermost to outermost
  ((domain-4-meaning

    ;;; Meaning from language structure
    ((domain-3-language

      ;;; Language from logic
      ((domain-2-logic

        ;;; Logic from numbers
        ((domain-1-numbers

          ;;; Numbers from Tesla core
          ((tesla-core
            (spark)
            (energy)
            ((spark . energy))))

          (numbers-live)
          ((tesla-core . numbers-live))))

        (logic-live)
        ((domain-1-numbers . logic-live))))

      (language-live)
      ((domain-2-logic . language-live))))

    (meaning-live)
    ((domain-3-language . meaning-live))))

  ;;; Target: the continuum is alive
  (continuum-alive)

  ;;; Vacuum tube: the chain that proves the cascade
  ((domain-4-meaning . continuum-alive)))
