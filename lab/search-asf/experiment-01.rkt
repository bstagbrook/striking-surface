#lang strike

;;; ═══════════════════════════════════════════════════════════════
;;; ATOMIC STATE FLOW — STRIKING SURFACE
;;;
;;; Alphabet: ( creation   ) fulfillment
;;; Primitives: () presence   (()) containment
;;; Transformation: ((()) (()) (())) — source, target, vacuum tube
;;;
;;; CONSTITUTIONAL: Shape determines what comes next. Not the clock.
;;; ═══════════════════════════════════════════════════════════════

;;; PURPOSE: Search as a strike table. All known facts about
;;; "gold" in one vacuum tube. The query IS the source.
;;; The matches ARE the residue.

;;; All gold-related pairs from across all registered domains,
;;; gathered into one vacuum tube. The query "gold" enters,
;;; the current follows every wire that starts with "gold."

(search-gold
  (gold)
  (everything-known-about-gold)
  (;;; FROM 005-relativistic
   (gold . 6s-contracts)
   (6s-contracts . 5d-expands)
   (5d-expands . gap-2.4eV)
   (gap-2.4eV . absorbs-blue)
   (absorbs-blue . yellow)
   ;;; FROM 015-periodic-table
   (gold . Z=79-5d10-6s1)
   ;;; FROM 019-cross-domain
   (gold . Z=79-heavy-nucleus)
   ;;; FROM 017-color-vision
   (gold . absorbs-blue-photons)))
